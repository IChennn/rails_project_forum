<div class="banner">
    <div class="container">

        <div class="row">
            <div class="col-lg-6">
                <h2>Enrich Your Life With Books:</h2>
            </div>
            <div class="col-lg-6">
                <ul class="list-inline">
                    <div class="group">
                      <%= link_to("Add New Book", new_group_path, class: "btn btn-lg btn-default pull-right") %>
                    </div>                       
                </ul>
                </div>
        </div>
    </div>
    <!-- /.container -->
  </div>
<!-- /.banner -->
  
<div class="col-md-12" id="review_body">
  <%= form_tag groups_path, id: "search-form", :method => "get" do %>  
  <%= submit_tag "Search", class: 'btn btn-default pull-right', name: nil %>
  <%= text_field_tag :search, params[:search], class: 'btn btn-default pull-right', placeholder: "Search Books" %>    
  <% end %>
</div>

<!--review body-->
<div class="col-md-12" id="review_body">
  
  <!-- <table class="table table-hover">
    <thead>
      <tr>
        <td></td>
        <td><h4>Title</h4></td>
        <td><h4>Description</h4></td>
      </tr>
    </thead>
    <tbody>
      < % @groups.each do |group| %>
        <tr>
          <td></td>
          <td>< %= link_to(group.title, group_path(group)) %></td>
          <td>< %= simple_format(group.description) %></td>
          <td>
              < % if current_user %>
                < %= link_to("Edit", edit_group_path(group), class: "btn btn-sm btn-default")%>
                < %= link_to("Delete", group_path(group),    class: "btn btn-sm btn-default", 
                          method: :delete, data: { confirm: "Are you sure?" } )%>
              < % end %>
          </td>
        </tr>
      < % end %>
    </tbody>
  </table> -->

  <% @groups.each do |group| %>
      <div class="row container">
            <div class="col-md-5">
                <div class="img-fluid rounded mb-3 mb-md-0">                    
                    <%= image_tag group.image.thumb %>
                </div>
            </div>
            <div class="col-md-7">
                <h3 class="text-primary"><%= group.title %></h3>
                <hr>
                <p><%= simple_format(group.description) %></p>
                <% if current_user %>
                <%= link_to("Edit", edit_group_path(group), class: "btn btn-md btn-default")%>
                <%= link_to("Delete", group_path(group),    class: "btn btn-md btn-default", 
                          method: :delete, data: { confirm: "Are you sure?" } )%>
                <% end %>
                <%= link_to("See More Reviews >>", group_path(group),class: "btn btn-md btn-primary") %>
            </div>
      </div>
      <hr>
  <% end %>

  <div class="text-center">
    <%= will_paginate @groups %>
  </div>

</div>

